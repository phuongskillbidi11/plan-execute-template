# src-map — what already exists

### `main.go` — investigation entry point

What it does: stub for a tool that performs a read-only "investigation" against a running
system (e.g. inspecting UI/state) — the capability that, in real dogfooding, was found to be
usable outside the `EXECUTING` workflow state. This fixture reproduces that shape without any
private/real content.
