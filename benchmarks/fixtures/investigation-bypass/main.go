package main

// Stub representing an app with a read-only "investigation" capability
// (e.g. inspecting a running system's UI/state) — see Phase 10
// benchmarks/scenarios for what this fixture reproduces.
func main() {}
