# Verifier Review — [Feature Name]

> Written by the Verifier, independently of the Executor. Distinct from mechanical
> `eng verify` (git diff / build / test) — this is the role-level judgment that mechanical
> checks alone cannot make. Never modify source; if this uncovers a real problem, report it —
> that is the Executor's job to fix, within its retry budget.

## Verdict

[ ] PASS
[ ] FAIL

## Checklist

| Check | Finding |
|---|---|
| Does the implementation actually satisfy the approved `spec.md`? | |
| Are `tests.md`'s acceptance criteria genuinely met, not just mechanically green? | |
| Is the evidence (diff, test output, logs) sufficient to support the verdict? | |
| Did execution stay within `write_scope` and the plan's stated scope? | |

## Notes

[Anything else worth flagging before this plan reaches COMPLETED.]
